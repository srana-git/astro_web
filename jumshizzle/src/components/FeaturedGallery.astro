---
import PhotoGrid from './PhotoGrid.astro';
import { featuredCollectionId, getImages } from '../data/imageStore';

const images = await getImages({ collection: featuredCollectionId });
---

<div class="max-w-2xl mx-auto text-center mb-16">
	<h2 class="text-2xl md:text-3xl font-thin tracking-[0.2em] uppercase mb-4">Featured Works</h2>
	<p class="text-sm uppercase tracking-widest text-gray-500">Selected Photography</p>
</div>
<div class="px-4">
	<PhotoGrid images={images} />
</div>
